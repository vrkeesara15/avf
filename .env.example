# Client (Vite) environment.
# Leave VITE_API_URL empty to use same-origin "/api" (works with the dev proxy
# and the production Firebase Hosting → Cloud Run rewrite).
VITE_API_URL=

# Where the Vite dev proxy forwards /api during `npm run dev`.
VITE_API_TARGET=http://localhost:8080
