rules_version = '2';

// All data access goes through the backend (Cloud Run) using the Firebase
// Admin SDK, which bypasses these rules. Direct client access is denied.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
